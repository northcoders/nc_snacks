const createRef = require('../utils.js');

describe("createRef", () => { 
    it("returns the relevant key-value pair when passed a single-item array", () => { 
        expect(createRef([]))
    })
})