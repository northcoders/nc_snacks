{
  "name": "be-node-postgres",
  "version": "1.0.0",
  "description": "We're making an api to keep track of all the tasty treats that the northcoders eat throughout the day. Some setup has been done to write a seed script to populate the database and your task is to create the first end point.",
  "main": "app.js",
  "scripts": {
    "test": "npm run setup-db && PGDATABASE=nc_snacks jest",
    "setup-db": "psql -f db/nc_snacks.sql",
    "dev": "PGDATABASE=nc_snacks nodemon listen.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^28.1.0",
    "nodemon": "^2.0.16",
    "supertest": "^6.2.3"
  },
  "dependencies": {
    "express": "^4.18.1",
    "pg": "^8.8.0"
  }
}
